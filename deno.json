{
  "tasks": {
    "build": "deno run -A build.ts",
    "run-node": "deno task build && node ./dist/mod.js",
    "run-bun": "deno task build && bun ./dist/mod.js",
    "publish": "deno task build && cd dist && npm publish"
  }
}